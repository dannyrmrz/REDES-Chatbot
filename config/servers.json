{
  "_comment": "MCP servers used by the host. Paths starting with ./ are resolved relative to this file. ${VARS} in urls come from .env.",
  "servers": [
    {
      "name": "filesystem",
      "description": "Official Anthropic Filesystem MCP server (Node, via npx).",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "../workspace"],
      "enabled": true
    },
    {
      "name": "git",
      "description": "Official Anthropic Git MCP server (Python module).",
      "command": "python",
      "args": ["-m", "mcp_server_git"],
      "enabled": true
    },
    {
      "name": "clinic",
      "description": "Our own MCP server over stdio: clinic appointment booking.",
      "command": "python",
      "args": ["-m", "clinic_server"],
      "cwd": "./..",
      "enabled": true
    },
    {
      "name": "clinic-remote",
      "description": "The same clinic server, deployed to the cloud over HTTP. Set CLINIC_REMOTE_URL in .env and flip enabled to true.",
      "transport": "http",
      "url": "${CLINIC_REMOTE_URL}",
      "enabled": false
    }
  ]
}
